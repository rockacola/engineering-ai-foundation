# 2026-06-16

## Summary

Applied the Engineering AI Foundation to itself — the repo now eats its own dogfood. Followed the `bootstrap/create-new-project.md` workflow adapted for `repo_type: migrated`, then trimmed the agent set to match what this repo actually needs.

## Changes Made

- Created full `.ai/` directory structure per `spec/repo-structure.yaml`
- Added `AGENTS.md` at repo root (from `templates/AGENTS.md`)
- Added `.aiignore` covering secrets, node_modules, and build artifacts
- Created manifests: `foundation.yaml`, `context.yaml`, `ownership.yaml`, `agents.yaml`
- Created context files: `product.md` and `architecture.md`
- Created agent files for all 7 registry agents, then removed `planner` and `qa`
- Added `docs/adr/0001-apply-foundation-to-itself.md`
- Added `.ai/standards/inherited.md` (no project-specific overrides)

## Decisions

- `repo_type: migrated` — this is an existing repo, not a new one
- Removed `planner` agent: this repo rarely has enough parallel work to warrant task decomposition
- Removed `qa` agent: "QA" here is just running `audit-project.md`, which reviewer/implementer can do inline
- Kept `release` agent: the spec is versioned and CHANGELOG exists, so version bumps benefit from a defined checklist

## Open Questions

- Should `spec/agent-registry.yaml` note which agents are optional vs. required for different repo types?

---

# Journal — 2026-06-16 (session 2)

## Summary

Simplified the foundation spec: merged two thin spec files, removed drifted template copies, corrected stale ADR. Bumped to 1.0.1.

## Changes Made

- Merged `spec/versioning.yaml` + `spec/migration-rules.yaml` into `spec/governance.yaml`
- Removed `templates/.ai/standards/coding.md`, `testing.md`, `security.md` — drifted copies replaced by bootstrap step 7a copying from `standards/` directly
- Removed redundant `.gitkeep` files from `templates/.ai/skills/` and `templates/.ai/memory/` (directories already have real content files)
- Updated all references to renamed/merged spec files: `bootstrap/upgrade-existing-project.md`, `.ai/agents/release.md`, `.github/PULL_REQUEST_TEMPLATE.md`, `migration-guides/README.md`, `CHANGELOG.md`
- Corrected `docs/adr/0001-apply-foundation-to-itself.md` — said "all seven agents" but actual final state is five (planner and qa omitted with rationale)
- Updated `.ai/memory/known-issues.md` to reflect that standards-drift is resolved
- Bumped `foundation_version` to 1.0.1 in `.ai/manifests/foundation.yaml`

## Decisions

- 1.0.1 patch (not 1.1.0): no schemas changed, no required folders renamed, no agents added or removed — purely corrections and structural cleanup per `spec/governance.yaml` versioning rules

## Open Questions

- None.
