{
  "name": "engineering-ai-foundation",
  "version": "1.1.1",
  "private": true,
  "scripts": {
    "check": "npm run format",
    "format": "prettier --write \"**/*.{md,yaml}\""
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
